# Dev Log — March 17, 2026

**When resuming work:** Read `SESSION_HANDOFF.md` first. Then this log and `docs/FLIGHT_PLAN.md` / `docs/00_RESEARCH_STUDIO_SPEC.md` as needed.

---

## Summary

Record of developments since the last dev log (March 13): production deploy of Today/Entries/prompts features and design docs; then repeated frontend-only commits updating the About and Philosophy public pages.

---

## Work Completed (since March 13 dev log)

### 1. Commit and push to production (backend + frontend)

Per `docs/COMMIT_AND_PUSH.md`, backend was deployed first, then frontend.

- **Backend (davidlong-tech-backend):** Committed and pushed `app.py` with:
  - `POST /api/branches`, `POST /api/mysteries` (create with slugified handles)
  - `GET /api/prompts` (list), `POST /api/prompts` (create custom), `PATCH /api/prompts/<id>` (edit text)
  - `GET /api/entries/today` now returns `{ entry, prompt }` — prompt is the entry’s when present, else random fallback
  - `PATCH /api/entries` accepts `research_prompt_id`
  - Pushed to `origin main` and `heroku main` (Heroku release v16).

- **Frontend (davidlong.tech):** Committed and pushed:
  - **Today:** Show entry’s prompt when a today entry exists; “Replace prompt” and “Edit” under the prompt box; “New prompt” link next to the Today heading
  - **Entries:** “Add branch” and “Add mystery” under Branch/Mystery dropdowns with dialogs
  - **ABCs of AI:** Page at `/studio/abc` (no nav link)
  - **Docs:** `FEATURE_ADDITIONS.md`, `BACKEND_AI_PARAMETERS.md`, `TODAY_PAGE_BEHAVIOR.md`, `SESSION_HANDOFF.md`; updates to `00_RESEARCH_STUDIO_SPEC.md`, `FUNCTIONAL_SPEC_TESTS.md`, `COMMIT_AND_PUSH.md`, `FLIGHT_PLAN.md`
  - **E2E:** `e2e/today-entries.spec.ts` for Today prompt controls and Entries Add branch/mystery
  - Pushed to `origin main` (Vercel deploys from GitHub).

### 2. Public site content updates (frontend only)

Multiple follow-up commits to the public pages:

- **About page** (`app/about/page.tsx`): Content and/or formatting updates.
- **Philosophy page** (`app/philosophy/page.tsx`): Content and/or formatting updates (several small commits).

No backend or schema changes; no new features. Commits were pushed to `origin main` as “About and philosophy page updates” or “Philosophy page updates.”

---

## Files Touched / Added (since March 13)

### Frontend (davidlong.tech)

| Area | Path |
|------|------|
| App | `app/about/page.tsx`, `app/philosophy/page.tsx` (content updates) |
| Log | `DEV_LOG_2026-03-17.md` (this file) |

(Full list of files in the single large frontend deploy was already implied by the March 13 “Work Completed” and the commit message for `1aa8c63`.)

### Backend (davidlong-tech-backend)

- No changes in the “public page updates” phase; the only backend commit in this window was the one deploy above (`app.py`).

---

## Commits (March 17, 2026 and since last log)

### Backend

- **ab0a698** — Entries/Today: branches & mysteries create, prompts list/create/update, today returns prompt (pushed to origin and Heroku).

### Frontend

- **1aa8c63** — Today: prompt from entry, Replace/Edit/New prompt; Entries: Add branch/mystery; ABCs; docs and E2E.
- **57ef43f** — About and philosophy page updates.
- **0113a93**, **4f003f5**, **0a36ae5** — Philosophy page updates.
- **ba85c6f** — About and philosophy page updates.

---

## Current state

- **Backend:** Heroku at latest deploy (v16) with branches, mysteries, prompts (list/create/update), and today endpoint returning entry + prompt.
- **Frontend:** `main` at ba85c6f; Vercel deploys from GitHub. Today and Entries features and ABCs as in FEATURE_ADDITIONS; public About and Philosophy pages updated with recent content changes.
- **Untracked (not committed):** `docs/AI_parameters_test_results.md`, `docs/media/Fiverr Basic Kit.zip`, `docs/media/Fiverr Basic Kit/` — add in a future commit if desired.
