# Copy to .env.local for local development.
# When set, the frontend proxies /api/* to this URL instead of Heroku.
# Leave unset for production (Vercel uses Heroku via rewrites).

NEXT_PUBLIC_API_URL=http://localhost:5000
